name: Build Slides
on: [push]

jobs:
  build:
    name: Pandoc build
    runs-on: ubuntu-18.04
    steps:
      - uses: docker://pandoc/core:2.9
        with:
          entrypoint: /bin/sh
          args: ls
